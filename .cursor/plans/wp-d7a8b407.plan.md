<!-- d7a8b407-aaab-4186-956f-9313ef83af5a f8cf8fe0-b0c9-4fc1-a3b1-e8d359ebb39b -->
# WordPress Chatbot Integration

## Todo

- setup-backend-host: Deploy or tunnel the FastAPI service and note its base URL.
- prepare-assets: Adapt `static/chat.js` to accept a configurable API base and bundle CSS/JS for WordPress.
- build-plugin: Scaffold `wp-content/plugins/lunpetshop-chatbot` with plugin header, enqueue hooks, and footer markup injection.
- configure-wordpress: Upload/activate the plugin, set the API URL (hard-coded or via option), and verify the widget loads on production.

## Notes

- Reuse `static/chat.js` and `static/style.css`, adding a small wrapper so fetch points at the provided API endpoint rather than `/api/...`.
- Use `wp_enqueue_style`/`wp_enqueue_script` and `wp_localize_script` inside the plugin to pass the API base URL into the widget init code.
- Render the chat widget container by hooking `wp_footer` so it appears on every page without editing theme files.
- During testing, use the loca.lt tunnel URL; plan for a stable host after demo so the widget stays reachable.